# Hypothesis

The bundled v7 checkpoint may have useful ranking information even though its public confidence
is saturated. Raw logits can distinguish correct from incorrect decisions, and option-order
permutation can reveal a hidden schema sensitivity before another training run.
